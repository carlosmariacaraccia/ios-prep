## Week 1 - Class vs Struct
It's nice to review concepts even if i already know the outcome
The concept that it is always nice to review is that even if 
I already know it. It is nice to learn new vocabulary. 
In this case let makes the binding contant not the instance.
For value types the binding is the value, so making the binding constant 
freezes the values (cannot mutate them). For ref types the biding i
s a pointer to an instance in memory, So what it is freezing is the pointer
itself: "You will always point to this exact instance", that is why we 
cannot reassign the value.
